#include <stdio.h>

int main() {
    int choice;
    float num1, num2;

    do {
        printf("\n1. Addition\n");
        printf("2. Subtraction\n");
        printf("3. Exit\n");
        printf("Enter your choice: ");
        scanf("%d", &choice);

        if(choice == 1) {
            printf("Enter two numbers: ");
            scanf("%f %f", &num1, &num2);
            printf("Result: %.2f\n", num1 + num2);
        }
        else if(choice == 2) {
            printf("Enter two numbers: ");
            scanf("%f %f", &num1, &num2);
            printf("Result: %.2f\n", num1 - num2);
        }

    } while(choice != 3);

    return 0;
}
